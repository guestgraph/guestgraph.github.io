# What every member of the family ignores: installed packages, and the state an editor
# or a tool keeps beside the work.
node_modules
.vscode
.claudian
.superpowers

# package-lock.json is committed, deliberately: CI runs `npm ci`, which fails outright
# without a lockfile in the tree. It was ignored while nothing ran the suite automatically.
__pycache__/
